[package]
name = "stellarforge-payments"
description = "Reusable payment infrastructure for StellarForge: XLM and issued-asset payment building."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
stellarforge-core = { path = "../stellarforge-core" }
stellarforge-tx = { path = "../stellarforge-tx" }
